{
  "name": "dixit-core",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "immutable": "^3.8.2",
    "invariant": "^2.2.4",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/cucumber": "^4.0.4",
    "@types/uuid": "^3.4.3",
    "chai": "^4.1.2",
    "cucumber": "^4.2.1",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "find . -not -path './node_modules/*'  -type f -iname '*.js' -delete -o -not -path './node_modules/*' -iname '*.js.map' -delete",
    "test:game": "yarn build && cucumber-js features/"
  }
}
